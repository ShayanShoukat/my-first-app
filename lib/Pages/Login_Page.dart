import 'package:flutter/material.dart';

class LoginPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
  return Material(
  child: Center(
  child: Text("Loginpage",style: (
  TextStyle(
    color: Colors.red,
    fontStyle: FontStyle.italic,
    fontSize: 30,
  )
  ),),),
  );
  }
}
